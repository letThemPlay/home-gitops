apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: forgejo
resources:
  - ocirepository.yml
  - helmrelease.yml
  - tcproute.yml
#  - httproute.yml

configMapGenerator:
  - name: forgejo-config
    files:
      - values.yaml=values.yml

configurations:
  - kustomizeconfig.yml
