apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: continuwuity-ingress
  namespace: matrix
spec:
  ingressClassName: cloudflare
  rules:
  - host: matrix.frenchieandthegeek.com
    http:
      paths:
      - backend:
          service:
            name: continuwuity-service
            port:
              number: 80
        path: /
        pathType: ImplementationSpecific
