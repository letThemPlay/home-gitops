apiVersion: v1
kind: Service
metadata:
  name: continuwuity
  namespace: matrix
spec:
  clusterIP: "None"
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app.kubernetes.io/name: continuwuity
  type: ClusterIP
