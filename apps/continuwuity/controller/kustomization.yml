apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: matrix
resources:
  - namespace.yml
  - helmrepository.yml

configMapGenerator:
  - name: continuwuity-config
    files:
      - values.yaml=values.yml

configurations:
  - kustomizeconfig.yml
