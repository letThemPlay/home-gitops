apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: matrix
resources:
  - namespace.yml
  - pvc.yml
  - statefulset.yml
  - service.yml
  - ingress.yml

configMapGenerator:
  - name: continuwuity-env
    envs:
      - .env

configurations:
  - kustomizeconfig.yml
