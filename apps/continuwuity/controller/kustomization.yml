apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: matrix
resources:
  - namespace.yml
  - pvc.yml
  - statefulset.yml

configMapGenerator:
  - name: continuwuity-env
    files:
      - envs.yaml=envs.yml

configurations:
  - kustomizeconfig.yml
