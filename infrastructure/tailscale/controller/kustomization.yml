apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tailscale
resources:
  - helmrepository.yml
  - helmrelease.yml

configMapGenerator:
  - name: tailscale-config
    files:
      - values.yaml=values.yml

configurations:
  - kustomizeconfig.yml
